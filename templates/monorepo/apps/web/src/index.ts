// Frontend (Next.js 15 + Tailwind + shadcn/ui). Stub até o M1 — o time constrói via /a360-vamos.

export const WEB_NAME = '@app/web' as const;

/** Placeholder. O slot de frontend substitui por um app Next.js real lendo docs/design/raw/. */
export function describe(): string {
  return 'web stub — sem telas ainda. Rode /a360-vamos.';
}
