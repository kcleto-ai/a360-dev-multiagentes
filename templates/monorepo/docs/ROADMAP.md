# ROADMAP

> Milestones do produto + critérios de sucesso. O CTO/Arquiteto escrevem ANTES de construir
> (skill `decompose-goal`). Fonte da verdade do escopo de cada milestone.

## M1 — <nome>  (status: a definir)

**Objetivo:** <o caminho feliz que o M1 entrega>

**Critérios de sucesso (usados no HTC):**
- <o que o usuário consegue fazer ao final — observável>

**Slots (waves: fundação primeiro — ver PARALLEL-PROTOCOL):**

| Slot | Wave | Depende de | Território (resumo) |
|---|---|---|---|
| `<db-fundacao>` | 0 | — | `packages/db` |
| `<api-dominio>` | 1 | `<db-fundacao>` | `modules/<dominio>` |
| `<web-rota>` | 1 | `<db-fundacao>` | `app/<rota>` + `queries/<dominio>` |

**Dívida consciente deste milestone:**
- <atalho intencional> — pagar quando: <gatilho, ex.: "refatorar quando tocado no M2">
  (dívida sem dono e sem gatilho não é dívida consciente — é surpresa futura)

## M2+ (esboço)

- <o que fica pra depois>

---

## Histórico

| Milestone | Fechado em | HTC |
|---|---|---|
| — | — | — |
